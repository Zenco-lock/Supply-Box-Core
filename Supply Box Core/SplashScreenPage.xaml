<Page x:Class="Supply_Box_Core.SplashScreenPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Supply_Box_Core"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="SplashScreenPage">

    <!-- Grid principal que cobre toda a página com fundo branco -->
    <Grid Background="White">

        <!-- Logo da aplicação posicionado no centro -->
        <Image Source="Suply_Box_Core_Logo.jpg" 
               HorizontalAlignment="Center" 
               VerticalAlignment="Center" 
               Stretch="UniformToFill" />

        <!-- Grid secundário para a barra de progresso e o texto de carregamento -->
        <Grid HorizontalAlignment="Center" 
              VerticalAlignment="Bottom" 
              Margin="0,0,0,40">

            <!-- Barra de progresso indeterminada para indicar o carregamento -->
            <ProgressBar IsIndeterminate="True" 
                         Width="624" 
                         Height="37" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Center" />

            <!-- Texto "Loading..." para indicar visualmente que algo está a ser carregado -->
            <TextBlock Text="Loading..." 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Foreground="Black" 
                       FontSize="20" 
                       FontStyle="Italic"/>
        </Grid>

    </Grid>

</Page>